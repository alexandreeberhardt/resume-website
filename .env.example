# ===========================================
# CV Generator - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# SECURITY: Never commit .env to version control!

# --- PostgreSQL Database ---
# REQUIRED: These variables must be set (no defaults in docker-compose)
POSTGRES_USER=cvuser
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=cvdatabase

# Note: DATABASE_URL is automatically constructed in docker-compose.yml
# For local development without Docker, set it manually:
# DATABASE_URL=postgresql://cvuser:your_secure_password_here@localhost:5432/cvdatabase

# --- JWT Authentication ---
# REQUIRED: Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your_jwt_secret_key_here_min_32_chars
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- Google OAuth2 (optional, for "Login with Google") ---
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your_google_client_secret
GOOGLE_REDIRECT_URI=https://yourdomain.com/api/auth/google/callback

# --- CORS Configuration ---
# Comma-separated list of allowed origins for CORS
# SECURITY: Never use "*" in production
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# --- Frontend URL (for OAuth redirects) ---
FRONTEND_URL=https://yourdomain.com

# --- Mistral API (for CV import feature) ---
MISTRAL_API_KEY=your-mistral-api-key-here

# --- AWS S3 Storage ---
AWS_ACCESS_KEY_ID=AKIAXXXXXXXXXXXXXXXX
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_S3_BUCKET=your-cv-bucket-name
AWS_REGION=eu-west-3

# --- Application Settings (optional) ---
# WORKERS=4
# LOG_LEVEL=info

# --- Resource Limits (enforced in code) ---
# Guest users: max 3 resumes
# Registered users: max 50 resumes
# Resume JSON content: max 100 KB
# OAuth code expiration: 1 minute
# S3 presigned URLs: 5 minutes expiration
